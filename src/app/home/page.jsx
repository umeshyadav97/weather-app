import React from "react"
import { Box, Grid } from "@mui/material"

function Home() {
  return (
    <React.Fragment>
      <Grid style={{ background: "red" }}>
        This version defines a TypeScript interface Props to describe the props that Search
        component expects. It also defines Search component as a Function Component (FC) and
        annotates the props with their respective types.This version defines a TypeScript interface
        Props to describe the props that Search component expects. It also defines Search component
        as a Function Component (FC) and annotates the props with their respective types.This
        version defines a TypeScript interface Props to describe the props that Search component
        expects. It also defines Search component as a Function Component (FC) and annotates the
        props with their respective types.This version defines a TypeScript interface Props to
        describe the props that Search component expects. It also defines Search component as a
        Function Component (FC) and annotates the props with their respective types.This version
        defines a TypeScript interface Props to describe the props that Search component expects. It
        also defines Search component as a Function Component (FC) and annotates the props with
        their respective types.
      </Grid>
    </React.Fragment>
  )
}

export default Home
